# 🛡️ Next.js MCP Client UI: Okta & Shadcn/ui Setup

This guide details the secure implementation of the MCP Client UI using **Next.js**, **Shadcn/ui** for components, and **Okta-Auth-JS** for handling the Authorization Code Flow with PKCE.

## 1. ⚙️ Project Setup

### A. Install Dependencies

Install the Okta library, React hooks, and the `shadcn/ui` prerequisite.

```bash
npm install @okta/okta-auth-js @okta/okta-react react-router-dom
# Install required Shadcn dependency for server-side rendering support
npm install next-themes
```
